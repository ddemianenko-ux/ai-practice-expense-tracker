# AI Session Documentation

## Інструмент 1: Gemini (Google)
**Промпт 1 (Генерація базової структури проекту та логіки):**
"Створи CLI-калькулятор витрат.CLI-інструмент для відстеження особистих витрат. Функціональні вимоги: Додавання витрати, Перегляд витрат, Підрахунок суми, Збереження у JSON."

**Відповідь AI (Gemini):**
Gemini згенерував повний скрипт `expense_tracker.py` мовою Python, який включав функції `load_data()`, `save_data()`, `add_expense()`, `list_expenses()`, та `summarize_expenses()`. Також AI надав покрокову інструкцію для розгортання проекту та допоміг вирішити помилки терміналу.

## Інструмент 2: GitHub Copilot
**Дія (Парсинг аргументів командного рядка):**
Під час написання функції `main()`, я додав коментар: `# Setup argparse for add, list, and summary commands`.

**Запропонований код (прийнято частково):**
Copilot згенерував базову структуру для `argparse.ArgumentParser` та підкоманди. 
Я прийняв пропозицію за допомогою клавіші `Tab`, але вручну додав прапорець `--desc` для команди `add`, оскільки Copilot запропонував лише `--category` та `--amount`.