# AI Session Documentation

## Інструмент 1: ChatGPT (GPT-4)
**Промпт 1 (Генерація базової структури проекту):**
"Виступай як досвідчений Python розробник. Згенеруй базову структуру CLI-інструменту для відстеження особистих витрат. Мені потрібні функції для читання/запису JSON та підготовки до використання argparse."

**Відповідь AI (скорочено):**
"Ось базова структура для вашого проекту. Я створив функції `load_data()` та `save_data()`, які перевіряють наявність файлу `expenses.json` та записують у нього дані. Також додано порожню функцію `main()` для подальшого парсингу аргументів."

---

**Промпт 2 (Функції агрегації даних):**
"Напиши функцію `summarize_expenses`, яка зчитує масив словників з витратами та підраховує загальну суму, а також суму за кожною категорією. Використай `collections.defaultdict`."

**Відповідь AI:**
"Звісно! Ось функція, яка використовує `defaultdict` для ефективного групування сум: [код функції summarize_expenses]."

## Інструмент 2: GitHub Copilot
**Дія (Парсинг аргументів командного рядка):**
Я написав коментар: `# Setup argparse for add, list, and summary commands`.

**Запропонований код (прийнято частково):**
Copilot згенерував створення `argparse.ArgumentParser` та 3 підкоманди. 
Я прийняв пропозицію за допомогою клавіші `Tab`, але вручну додав прапорець `--desc`, оскільки Copilot запропонував лише `--category` та `--amount`.